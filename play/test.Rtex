\documentclass{article}

\begin{document}

<<sys_info>>=
temp <- Sys.info()
for (i in temp) { print(i) }
@

<<install_pkg>>=
#install.packages("RPostgreSQL_0.4.tar.gz", lib=".", repos=NULL)
system("git checkout -b slatex")
@


\end{document}